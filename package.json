{
  "name": "icloud-mcp",
  "version": "0.0.0",
  "private": true,
  "description": "Local iCloud Mail MCP",
  "type": "module",
  "packageManager": "bun@1.3.14",
  "scripts": {
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "lint": "eslint .",
    "mcp:http": "bun run src/transports/http.ts",
    "mcp:stdio": "bun run src/transports/stdio.ts",
    "typecheck": "tsc --noEmit",
    "test": "bun test",
    "test:mcp": "bun test tests/mcp",
    "quality": "bun run format:check && bun run lint && bun run typecheck && bun test"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@modelcontextprotocol/client": "2.0.0",
    "@types/bun": "^1.3.14",
    "eslint": "^10.8.0",
    "prettier": "^3.9.6",
    "typescript": "~6.0.3",
    "typescript-eslint": "^8.66.0"
  },
  "dependencies": {
    "@modelcontextprotocol/server": "2.0.0",
    "zod": "4.4.3"
  }
}
